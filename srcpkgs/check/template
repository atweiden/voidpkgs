maintainer="nox"
pkgname="check"
version=0.15.0
revision=1
short_desc="Unit Testing Framework for C"
homepage="https://libcheck.github.io/check/"
license="LGPL-2.1-or-later"
distfiles="https://github.com/libcheck/check/releases/download/$version/$pkgname-$version.tar.gz"
checksum="aea2e3c68fa6e1e92378e744b1c0db350ccda4b6bd0d19530d0ae185b3d1ac60"
build_style="gnu-configure"

check-devel_package() {
  short_desc+=" - development files"
  depends="$sourcepkg>=${version}_$revision"
  pkg_install() {
    vmove usr/include
    vmove "usr/lib/*.a"
    vmove "usr/lib/*.so"
    vmove usr/lib/pkgconfig
    vmove usr/share/aclocal
  }
}

# REMARKS:
#
# We want the prepared archive to forego texlive in hostmakedepends.

# vim: set filetype=sh foldmethod=marker foldlevel=0 nowrap:
