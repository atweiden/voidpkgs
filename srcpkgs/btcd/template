maintainer="Andy Weidenbaum <archbaum@gmail.com>"
pkgname="btcd"
version=0.12.0beta
revision=4
_gitrev="aa6e0f35703c1438cc45860ddc0c3f6e6c633c93"
short_desc="An alternative full node Bitcoin implementation written in Go"
hostmakedepends="git"
tags="btcsuite"
homepage="https://github.com/btcsuite/btcd"
license="ISC"
distfiles="https://github.com/btcsuite/btcd/archive/$_gitrev.tar.gz>$pkgname-$version.tar.gz"
checksum="afb52c30f491c4bf248b0f6d17c179d666c27acb4363e63b78f4e4e131b806c7"
wrksrc="$pkgname-$_gitrev"
build_style="go"
go_import_path="github.com/btcsuite/btcd"

post_build() {
  GO111MODULE="on" go install -v . ./cmd/...
}

pre_install() {
  # prepend btc to unqualified binaries
  for _bin in $(find "$GOPATH/bin"  \
                        -mindepth 1 \
                        -maxdepth 1 \
                        -type f ! -name "btc*" -printf '%f\n'); do
    mv "$GOPATH/bin/$_bin" "$GOPATH/bin/btc$_bin"
  done
}

post_install() {
  vlicense LICENSE
  vdoc CHANGES
  vdoc README.md
  vdoc sample-btcd.conf
  vcopy docs "usr/share/doc/$pkgname"
}

# vim: set filetype=sh foldmethod=marker foldlevel=0 nowrap:
