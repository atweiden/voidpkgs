maintainer="Andy Weidenbaum <archbaum@gmail.com>"
pkgname="nqp"
version=2019.11
revision=1
short_desc="Lightweight Perl 6-like environment for virtual machines, with MoarVM support"
depends="MoarVM-${version}_$revision"
makedepends+=" MoarVM"
makedepends+=" libatomic_ops-devel"
makedepends+=" libffi-devel"
makedepends+=" libuv-devel"
hostmakedepends="perl"
tags="perl6"
homepage="https://github.com/perl6/nqp"
license="Artistic-2.0"
distfiles="https://github.com/perl6/nqp/releases/download/$version/$pkgname-$version.tar.gz"
checksum="b47f911def8aafded041b079ac86e5df23b726c190664c3217c567835f481328"
build_style="configure"
configure_script="perl Configure.pl"
configure_args+=" --backends=moar"
configure_args+=" --prefix=/usr"
if [[ -n "$CROSS_BUILD" ]]; then
  configure_args+=" --sdkroot=$XBPS_CROSS_BASE"
  configure_args+=" --sysroot=$XBPS_CROSS_BASE"
fi
make_check_target="test"
nocross="yes"

# vim: set filetype=sh foldmethod=marker foldlevel=0 nowrap:
