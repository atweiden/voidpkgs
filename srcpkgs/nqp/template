maintainer="Andy Weidenbaum <archbaum@gmail.com>"
pkgname="nqp"
version=2018.10
revision=1
short_desc="A lightweight Perl 6-like environment for virtual machines, with MoarVM support"
depends="MoarVM-${version}_$revision"
makedepends="MoarVM"
hostmakedepends="perl"
tags="perl6"
homepage="https://github.com/perl6/nqp"
license="Artistic-2.0"
distfiles="https://rakudo.perl6.org/downloads/nqp/$pkgname-$version.tar.gz"
checksum="ea97fd1ff3f9cf3efe10bcff8795bc0a6d1e0f316eb213f061c8fc1585f891c7"
build_style="configure"
configure_script="perl Configure.pl"
configure_args+=" --backends=moar"
configure_args+=" --prefix=/usr"
if [[ -n "$CROSS_BUILD" ]]; then
  configure_args+=" --sdkroot=$XBPS_CROSS_BASE"
  configure_args+=" --sysroot=$XBPS_CROSS_BASE"
fi
make_check_target="test"
nocross="yes"

# vim: set filetype=sh foldmethod=marker foldlevel=0 nowrap:
