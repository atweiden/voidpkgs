# credit: https://www.archlinux.org/packages/extra/x86_64/openmp/
maintainer="Andy Weidenbaum <archbaum@gmail.com>"
pkgname="openmp"
version=7.0.0
revision=1
short_desc="LLVM OpenMP Runtime Library"
makedepends+=" elfutils-devel"
makedepends+=" libffi-devel"
hostmakedepends+=" ninja"
hostmakedepends+=" perl"
hostmakedepends+=" pkg-config"
homepage="https://openmp.llvm.org"
license="Custom"
distfiles="https://releases.llvm.org/$version/$pkgname-$version.src.tar.xz"
checksum="30662b632f5556c59ee9215c1309f61de50b3ea8e89dcc28ba9a9494bba238ff"
wrksrc="$pkgname-$version.src"
build_style="cmake"
configure_args+=" -DLIBOMP_ENABLE_SHARED=ON"
configure_args+=" -G Ninja"
case "$XBPS_TARGET_MACHINE" in
  *-musl)
    configure_args+=" -DLIBCXX_HAS_MUSL_LIBC=YES"
    ;;
esac
make_cmd="ninja"

do_install() {
  cd "${cmake_builddir:=build}"
  DESTDIR="$DESTDIR" ninja install
}

post_install() {
  vlicense LICENSE.txt
}

openmp-devel_package() {
  short_desc+=" - development files"
  depends="$sourcepkg>=${version}_$revision"
  pkg_install() {
    vmove usr/include
    vmove "usr/lib/*.so"
    # libgomp.so is also provided by libgomp; remove it to fix the conflict
    rm -f "$PKGDESTDIR/usr/lib/libgomp.so"
  }
}

# vim: set filetype=sh foldmethod=marker foldlevel=0 nowrap:
