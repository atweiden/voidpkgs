maintainer="nox"
pkgname="dcrseeder"
version=0.0.1pre
revision=4
_gitrev="200a2176299d2438343a656146c1bc2af412885d"
short_desc="Decred DNS seeder"
tags="decred"
homepage="https://github.com/decred/dcrseeder"
license="ISC"
distfiles="https://github.com/decred/dcrseeder/archive/$_gitrev.tar.gz>$pkgname-$version.tar.gz"
checksum="742e9e53fed819ee28f7b73f129fc6bb546d823a125b9a3869e71cd7d4ed51e2"
build_style="go"
go_import_path="github.com/decred/dcrseeder"
go_build_tags+=" netgo"
go_build_tags+=" osusergo"
go_ldflags="-buildid="
if [[ "$XBPS_TARGET_LIBC" == "musl" ]]; then
  go_ldflags+=" -extldflags=-static-pie"
  go_ldflags+=" -linkmode=external"
fi

# XXX: -buildmode=pie is unverified on *-musl, and non-x86_64 or aarch64
export GOFLAGS="-trimpath -buildmode=pie -mod=readonly -modcacherw"

post_install() {
  vlicense LICENSE
  vdoc README.md
}

# vim: set filetype=sh foldmethod=marker foldlevel=0 nowrap:
